<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Client Profile</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <div class="container">
        <h2>Client Profile</h2>
        <% if (client) { %>
            <p><strong>Full Name:</strong> <%= client.fullName %></p>
            <p><strong>Address:</strong> <%= client.address %></p>
            <p><strong>Phone Number:</strong> <%= client.phoneNumber %></p>
            <p><strong>Company:</strong> <%= client.company %></p>
            <p><strong>Job Title:</strong> <%= client.jobTitle %></p>
            <p><strong>Projects:</strong> <%= client.projects %></p>
            <a href="/client/edit" class="btn btn-primary">Edit Profile</a>
            <form action="/client/delete" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-danger">Delete Profile</button>
            </form>
        <% } else { %>
            <p>No profile found. Please create your profile.</p>
            <a href="/client/create-client" class="btn btn-primary">Create Profile</a>
        <% } %>
    </div>
</body>
</html>
